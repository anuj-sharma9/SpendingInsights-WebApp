# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/spending
DB_USER=spending
DB_PASSWORD=your-secure-password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Firebase Service Account (path or JSON content)
# Option 1: Path to file (local development)
FIREBASE_SERVICE_ACCOUNT=/path/to/ServiceAccount.json

# Option 2: JSON content (production - use this in Railway/Render)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

